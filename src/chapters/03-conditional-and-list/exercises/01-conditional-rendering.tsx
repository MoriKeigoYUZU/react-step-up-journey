// TODO: ログイン状態に応じてメッセージを切り替えるコンポーネント
// ヒント: 三項演算子 condition ? A : B を使おう
export function LoginStatus(_props: { isLoggedIn: boolean }) {
  return null;
}

// TODO: 管理者のときだけバッジを表示するコンポーネント
// ヒント: && 演算子を使おう（condition && <要素>）
export function AdminBadge(_props: { isAdmin: boolean }) {
  return null;
}
